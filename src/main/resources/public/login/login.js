/*
    Helper functions

 */
// small helper function for selecting element by id
let id = id => document.getElementById(id);

function alertError() {
        alert("Invalid username/password combination")
}


//id("submit").addEventListener("click", alertError());
